extends layout

block content
  ul 
    each message in messages
      div(class="message")
        h2 #{message.user}
        h3 #{message.text}
        time #{message.added}
      
  a(href="/new")
    button Add new message